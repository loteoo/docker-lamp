## Start stack
```shell
docker-compose up
```
## Stop stack

```shell
docker-compose down
```

## Delete all data
```shell
docker-compose rm -s -v
```

## PHP app:
http://localhost:8000

## phpmyadmin:
http://localhost:8080